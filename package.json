{
  "name": "centinel-flywheel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "check": "node scripts/check.mjs",
    "test:flow": "node scripts/integration.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.10"
  },
  "devDependencies": {
    "netlify-cli": "^23.9.1"
  },
  "engines": {
    "node": ">=20"
  }
}
